/// <reference types="node" />
import { IncomingHttpHeaders, IncomingMessage } from "http";
import { ParamsDictionary } from "./interfaces/RouteParameter";
export default class Request<TParam extends ParamsDictionary = ParamsDictionary, TBody extends any = any, TQuery extends Record<string, string | number> = any> {
    req: IncomingMessage;
    orginalUrl: string;
    method: string;
    url: string;
    params: TParam;
    isBrowserRequest: boolean;
    query: TQuery;
    headers: IncomingHttpHeaders;
    body: TBody;
    cookies: Record<string, string>;
    constructor(req: IncomingMessage);
    init(): Promise<this>;
}
